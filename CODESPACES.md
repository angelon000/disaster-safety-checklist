# GitHub Codespaces로 배포하기

GitHub Codespaces를 사용하면 **GitHub에서 바로 실행**하고 외부에서 접속할 수 있습니다!

## ✨ 특징

- ✅ **무료 사용** (월 60시간 무료)
- ✅ **클릭 한 번**으로 실행
- ✅ **자동 HTTPS** URL 생성
- ✅ **외부 접속** 자동 지원
- ✅ **설정 불필요** (이미 구성됨)

---

## 🚀 사용 방법

### 1단계: Codespace 생성

1. GitHub 저장소 접속:
   ```
   https://github.com/angelon000/disaster-safety-checklist
   ```

2. **초록색 `Code` 버튼** 클릭

3. **`Codespaces` 탭** 선택

4. **`Create codespace on master`** 클릭

   ![Codespace 생성](https://docs.github.com/assets/cb-138303/mw-1440/images/help/codespaces/new-codespace-button.webp)

5. 브라우저에서 VS Code가 열립니다! (30초~1분 소요)

---

### 2단계: 서버 실행

터미널에서 (자동으로 열림):

```bash
python dashboard/app.py
```

또는 간편 실행:
```bash
./run_server.sh
```

---

### 3단계: 외부 접속

서버가 실행되면 **자동으로 팝업**이 뜹니다:

```
Your application running on port 8000 is available.
[Open in Browser] [Make Public]
```

**`Open in Browser`** 클릭하면 공개 URL로 접속됩니다!

```
https://xxxx-8000.app.github.dev
```

이 URL을 친구들과 공유하면 누구나 접속 가능합니다! 🎉

---

## 📱 포트 공개 설정

만약 팝업이 안 뜨면 수동으로 공개:

1. VS Code 하단 **`PORTS`** 탭 클릭
2. `8000` 포트 우클릭
3. **`Port Visibility`** → **`Public`** 선택
4. **`Open in Browser`** 클릭

---

## ⚡ 빠른 팁

### 자동 실행 설정

`.devcontainer/devcontainer.json`에 추가되어 있어서:
- 필요한 패키지 자동 설치됨
- 포트 8000 자동 오픈
- Python 환경 자동 설정

### Codespace 관리

**일시정지/재개**
- 30분 동안 활동이 없으면 자동 일시정지
- 다시 접속하면 자동 재개

**삭제**
- GitHub → Settings → Codespaces → 삭제

**무료 사용량**
- 개인 계정: 월 60시간 무료
- 2-core: 120시간
- 4-core: 60시간

---

## 🔄 업데이트 배포

코드를 수정하고 GitHub에 푸시하면:

1. Codespace에서 `git pull`
2. 서버 재시작 (`Ctrl+C` 후 다시 실행)

---

## 💡 장점

| 기능 | Codespaces | ngrok | 로컬 |
|------|-----------|-------|------|
| 무료 | ✅ 60시간/월 | ✅ 2시간/세션 | ✅ |
| 외부 접속 | ✅ 자동 | ✅ | ⚠️ 방화벽 설정 |
| HTTPS | ✅ 자동 | ✅ | ❌ |
| 설정 시간 | ⚡ 1분 | ⚡ 5분 | 🐢 10분+ |
| 영구 URL | ❌ | ❌ | ⚠️ |
| 서버 불필요 | ✅ | ❌ | ❌ |

---

## 🎯 언제 사용하면 좋을까?

✅ **이럴 때 사용하세요:**
- 빠르게 데모를 보여주고 싶을 때
- 별도 서버가 없을 때
- 팀원들과 테스트할 때
- 개발 중 실시간 협업할 때

❌ **이럴 땐 다른 방법:**
- 24시간 운영 서비스 → AWS/GCP/Azure
- 영구 URL 필요 → 클라우드 배포
- 대용량 트래픽 → 전용 서버

---

## 🆘 문제 해결

### 포트가 안 열려요
```bash
# 터미널에서 확인
lsof -ti:8000

# 8000번 포트가 사용 중이면
kill -9 $(lsof -ti:8000)
python dashboard/app.py
```

### 패키지가 안 설치돼요
```bash
pip install -r requirements.txt
```

### Git pull이 안 돼요
```bash
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
git pull
```

---

## 📚 더 알아보기

- [GitHub Codespaces 문서](https://docs.github.com/en/codespaces)
- [VS Code in Browser](https://code.visualstudio.com/docs/remote/codespaces)
- [포트 포워딩](https://docs.github.com/en/codespaces/developing-in-codespaces/forwarding-ports-in-your-codespace)

---

## 🎉 시작하기

지금 바로 시작하세요!

**→ [Codespace 만들기](https://github.com/angelon000/disaster-safety-checklist/codespaces/new)**

클릭 한 번으로 1분 만에 실행됩니다! 🚀
